# 2026-02-25_00-56-01 로고/개미 배경제거 및 표시 보정

## 작성시각
2026-02-25 00:56:01

## 해결하고자 한 문제
- 랜딩 상단 ANT GRAVITY 로고가 작거나 잘려 보이는 문제.
- 대시보드 개미 캐릭터에 배경이 보여 캐릭터만 보이지 않는 문제.

## 진행 내용
- 나노바나나로 생성한 가로형 로고/개미 이미지를 기반으로 체크 배경 제거 및 크롭 처리.
- `frontend/public/branding/ant-gravity-wordmark.png` 재생성(한 줄 워드마크 중심 크롭).
- `frontend/public/branding/ant-mascot-v3.png` 재생성(개미만 남기도록 배경 제거 및 크롭).
- 랜딩 헤더 로고 렌더 크기/클래스 조정.
- 대시보드 개미 이미지 렌더에서 테두리/배경 느낌 제거하고 캐릭터 중심 표시로 조정.

## 해결된 것
- 로고가 한 줄로 더 크게 읽히도록 보정 완료.
- 개미 캐릭터가 배경 없이 캐릭터만 보이도록 보정 완료.

## 해결되지 않은 것
- 없음.

## 검증
- `pnpm.cmd lint` 통과
- `pnpm.cmd typecheck` 통과
- `python -m compileall backend/app` 통과
