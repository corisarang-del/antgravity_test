# UTF-8 인코딩 파싱 오류 핫픽스

- 작성시각: 2026-02-24 22:30:50
- 해결하고자 한 문제:
  - `src/app/page.tsx` 파싱 시 `invalid utf-8 sequence` 빌드 에러 발생
  - 랜딩 CTA에서 `/dashboard` 링크를 사용하지만 app 라우팅 브리지 파일이 없어 경로 불일치 위험

- 해결된 것:
  - `src/app/page.tsx`, `src/app/basis/page.tsx`, `src/app/history/page.tsx`, `src/app/watchlist/page.tsx`를 UTF-8로 재저장
  - `app/dashboard/page.tsx` 브리지 파일 추가해 `/dashboard` 경로 연결
  - `pnpm.cmd lint`, `pnpm.cmd typecheck` 통과

- 해결되지 않은 것:
  - 현재 환경 네트워크 제한으로 Google Fonts fetch 실패(`next build`)
